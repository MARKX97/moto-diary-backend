## Local API server env template (for `pnpm run dev:api`)
## Copy to .env.local and adjust values.

API_LOCAL_HOST=127.0.0.1
API_LOCAL_PORT=3100

## Sentry (local default: do not set SENTRY_FORCE_ENABLE)
SENTRY_ENABLED=true
SENTRY_DSN=
APP_ENV=dev
SENTRY_ENV=local
# SENTRY_FORCE_ENABLE=true

